package Lotto;

import java.util.Scanner;

/* --menu--
 * 1. 로또 번호 생성(수동) : 사용자가 입력
 * => creatLotto(scan);
 * 2. 로또 번호 생성(자동) : random(6자리)
 * => createLottoAuto()
 * 3. 당첨번호 생성(자동) : random(7자리) => 배열에 저장
 * => insertLottoAuto()
 * 4. 당첨번호 (내역)확인 : (현재 회차 당첨번호가 가장 위에 출력) 1개만 확인 => 등수체크
 * => checkLotto()
 * 5. 당첨번호 리스트 확인 : 전부 (마지막 당첨번호가 가장 위에 출력)
 * => printLotto()
 * 6. 종료
 * 
 * 
 * 1. Lotto class: 6개의 배열 생성하는 로또 클래스 (+수동 생성에 대한 메서드)
 * 2. LottoE class : 기존 로또 클래스를 상속 받아 처리 (당첨번호를 저장하는 배열 저장) => 기존 배열(6자리) int bonus만 처리
 * 3. LottoManager : 메뉴의 메서드 처리
 * 4. LottoMain : 메뉴 입력받아 처리
 * */


public class Lotto { 
	private int creatLotto[] = new int[6]; //나의 로또번호생성
	private int insertLotto[] = new int[7]; //당첨번호생성
	
	//로또번호 자동 생성(6자리)
	public static int random() {
		return (int)(Math.random()*45)+1;
		}
	//로또번호 자동 생성한 것을 배열에 넣기
	public static void createLottoAuto(int arr[]) {
		//중복없이 채우기
	   for(int i=0; i<arr.length; i++) {
		   int r = random();
		   for(int j=0 ; j<i ; j++) {
		   if(arr[i] != r) { //없다면..
			   arr[i] = r;
		   } else {
			   //있다면..
			  i--; 
		   }
	   }
		}
	}
	//당첨번호 자동 생성(7자리)
		public static int random1() {
			return (int)(Math.random()*45)+1;
			}
	//당첨번호 자동 생성한 것을 배열에 넣기
		public void insertLottoAuto(int arr[]) {
			//중복없이 채우기
		   for(int i=0; i<arr.length; i++) {
			   int r = random1();
			   for(int j=0 ; j<i ; j++) {
			   if(arr[i] != r) { //없다면..
				   arr[i] = r;
			   } else {
				   //있다면..
				  i--; 
			   }
		   }
			}
		}


	
	
	
	



}
